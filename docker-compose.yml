services:
  learn-aziral:
    build: .
    container_name: learn-aziral
    restart: always
    networks:
      - fenix_fenix-network
      - fenix-global-network

networks:
  fenix_fenix-network:
    external: true
  fenix-global-network:
    external: true
